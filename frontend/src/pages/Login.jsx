import AuthPlaceholder from '../components/AuthPlaceholder.jsx'

export default function Login() {
  return (
    <AuthPlaceholder
      title="Login"
      note="Owned by M1 (Plan §6.1) — POST /api/v1/auth/login."
    />
  )
}
